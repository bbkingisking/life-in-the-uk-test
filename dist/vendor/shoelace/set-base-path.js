// Stable re-export so index.html doesn't have to hardcode Shoelace's own
// content-hashed chunk filename (see vendor/shoelace/README.md - "Updating,
// or adding another component" step 4 also covers this file). Not part of
// Shoelace's own build; hand-written.
export { setBasePath } from './chunks/chunk.3Y6SB6QS.js';
